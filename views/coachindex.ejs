<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
			rel="stylesheet" />
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
		<title>Coaches</title>
	</head>
	<body class="background">
		<style>
			.background{
				background-color: lightblue;
			}</style>
		<%- include ('partials/navbar.ejs') %>
		<tbody>
			<% for(let i=0; i < product.length; i++) { %>
				<tr>
					<td>
						<a href="/product/<%= product[i].productId %>">
							<%=product[i].productName%>
						</a>
					</td>
					<td><img src="images/<%= product[i].image %>" width="20%" /></td>
					<td>
						<%= product[i].quantity %>
					</td>
					<td>$<%= product[i].price.toFixed(2) %>
					</td>
					<td><a href="/editProduct/<%= product[i].productId %>">Edit</a></td>
					<td><a href="/deleteproduct/<%= product[i].productId %>"
							onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
				</tr>
				<% } %>
		</tbody>
	</body>
</html>
