<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>ShuttleStore</title>
</head>
<body class = 'background'>
    <style>
		.background{
            background-color: lightblue;
        }</style>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2></h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Coach ID</th>
                    <th>Coach Name</th>
                    <th>Coach Gender</th>
                    <th>Coach Description</th>
                    <th>Coach Gender</th>
                    <th>Coach Age</th>
                    <th>Coach Fee</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < coach.length; i++) {%>
					<tr>
						<td><%= coach[i].coach_id %></td>
						<td><a href="/coach/<%= coach[i].coach_id%>"><%= coach[i].coach_name %></a></td>
                        <td><img src="images/<%= coach[i].coach_image %>" width="100%" /></td>
						<td><%= coach[i].coach_description %></td>
                        <td><%= coach[i].coach_gender %></td>
                        <td><%= coach[i].coach_age %></td>
                        <td><%= coach[i].coach_fee %></td>
						<td><a href="/editCoach/<%= coach[i].coach_id %>">Edit</a></td>
						<td><a href="/deleteCoach/<%= coach[i].coach_id %>"
								onclick="return confirm('Are you sure you want to delete this coach?')">Delete</a>
						</td>
					</tr>
					<% } %>
            </tbody>
        </table>
    </div>
</body>
</html>