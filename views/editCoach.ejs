<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>ShuttleStore</title>
</head>

<body class ='background'>
    <style>
		.background{
            background-color: lightblue;
        }</style>
    <%- include ('partials/navbar.ejs') %>
        <div class="container mt-3">
            <h2>Update Coaches</h2>
            <% if (coach) { %>
                <form action="/editCoach/<%= coach.coach_id %>" method="POST" enctype="multipart/form-data">
                    <label for="coach_name">Coach Name:</label><br>
                    <input type="text" id="coach_name" name="coach_name" value="<%= coach.coach_name %>" required><br><br>

                    <label for="coach_gender">Coach gender:</label><br>
                    <input type="text" id="coach_gender" name="coach_gender" value="<%= coach.coach_gender %>" required><br><br>

                    <label for="coach_age">Coach Age:</label><br>
                    <input type="number" id="coach_age" name="coach_age" min="0" value="<%= coach.coach_age %>" required><br><br>

                    <label for="coach_fee">Price:</label><br>
                    <input type="number" id="coach_fee" name="coach_fee" min="0" step="0.01" value="<%= coach.coach_fee %>"
                        required><br><br>
                    
                    <label for="current_image">Current Image:</label><br>
                    <input type="text" id="current_image" name="current_image" value="<%= coach.coach_image %>"
                        disabled><br>
                    <img src="/images/<%= coach.coach_image %>" width="20%"><br><br>

                    <label for="coach_image">New Image:</label><br>
                    <input type="file" id="coach_image" name="coach_image" accept="image/*"><br><br>

                    <label for="coach_description">Description:</label><br>
                    <textarea id="coach_description" name="coach_description" rows="4" cols="50"
                        required><%= coach.coach_description %></textarea><br><br>

                    <button type="submit">Update Product</button>
                </form>
                <% } else { %>
                    <p>No coach found.</p>
                    <% } %>

                    <br><h4><a href="/coach">Back</a></h4>
        </div>
</body>

</html>